<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ReviewIT.LibraryPage"
             Title="Your Library">
    <VerticalStackLayout
        Padding="30,0"
        Spacing="25">

        <CollectionView x:Name="displayLibrary">
            <CollectionView.ItemsLayout>
                <LinearItemsLayout
                    Orientation="Vertical"
                    ItemSpacing="10"/>
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Padding="15" BackgroundColor="#4d3ebc" >
                        <VerticalStackLayout>

                            <Label  
                                Text="{Binding question}"
                                FontSize="18"
                                Padding="10"/>

                        </VerticalStackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Button
            Text="Return"
            Clicked="OnReturn"/>

    </VerticalStackLayout>
</ContentPage>